{{>layout/header}}

<input type="hidden" id="list" value="{{array}}">

<div class="container" style="margin-top:30px">
    <div class="container">
        <h2 class="center">Admin Page</h2>
        <h2 class="center">방문자 수</h2>

        <div class="container">
            <canvas id="myChart"></canvas>
        </div>

        <div class="row">
            <div class="col-sm-4">
                <button type="button" class="btn btn-outline-info" onclick="location.href='/admin/user'">권한 부여</button>
            </div>
            <div class="col-sm-4">
                <button type="button" class="btn btn-outline-info" onclick="alert('전체 회원 수 : {{count}}');return false">전체 회원 수</button>
            </div>
        </div>
    </div>
</div>

{{>layout/admin_footer}}